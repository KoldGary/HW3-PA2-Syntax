# Meta-grammar for PLCC lexical specifications
# Students should complete this grammar  parse PLCC lexical specifications

# TODO: Add lexical specification here
skip WS '\s+'
skip COMMENT '#.*'
token SKIP 'skip'
token TOKEN 'token'
token Name '[A-Z0-9_]'
token PATTERN '\' [^\']*\''

%
# TODO: Add syntactic specification here
<spec> ::= <lines>
<lines> ::= <line> <lines> | /*empty*/
<line> ::= <rule> | COMMENT
<rule> ::= SKIP NAME PATTERN | TOKEN NAME PATTERN | NAME PATTERN

%
# Semantic specification will be added in Q7